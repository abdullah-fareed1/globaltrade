<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>GlobalTrade</display-name>

    <!--
        Phase 6 (build plan Step 6A), revised per DEVIATION_LOG_SECURITY.md.

        NO login-config element here. That block is superseded by the
        FormAuthenticationMechanismDefinition annotation on
        lk.globaltrade.web.security.SecurityConfig (globaltrade-web
        module) - see that class's Javadoc. Adding a login-config block
        here as well would be redundant at best and a source of
        confusing double configuration at worst; the annotation is the
        sole source of truth for the FORM mechanism, login.jsp and
        error.jsp locations.

        security-constraint and security-role are UNAFFECTED by that
        change and remain required here: Jakarta Security enforces
        authorization against the roles declared and constrained in
        web.xml, it does not replace this part.
    -->

    <security-constraint>
        <display-name>Customer area</display-name>
        <web-resource-collection>
            <web-resource-name>customer</web-resource-name>
            <url-pattern>/customer/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>CUSTOMER</role-name>
        </auth-constraint>
        <user-data-constraint>
            <transport-guarantee>NONE</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

    <security-constraint>
        <display-name>Coordinator area</display-name>
        <web-resource-collection>
            <web-resource-name>coordinator</web-resource-name>
            <url-pattern>/coordinator/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>COORDINATOR</role-name>
        </auth-constraint>
        <user-data-constraint>
            <transport-guarantee>NONE</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

    <security-constraint>
        <display-name>Admin area</display-name>
        <web-resource-collection>
            <web-resource-name>admin</web-resource-name>
            <url-pattern>/admin/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>ADMIN</role-name>
        </auth-constraint>
        <user-data-constraint>
            <transport-guarantee>NONE</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

    <!--
        /home is the post-login router (CONTRACTS.md Sec11 / build plan
        FIX 11): reachable by any authenticated role, it inspects
        request.isUserInRole(...) and redirects to the matching
        dashboard. It must stay open to all three roles, not just one.
    -->
    <security-constraint>
        <display-name>Authenticated landing page</display-name>
        <web-resource-collection>
            <web-resource-name>home</web-resource-name>
            <url-pattern>/home</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>CUSTOMER</role-name>
            <role-name>COORDINATOR</role-name>
            <role-name>ADMIN</role-name>
        </auth-constraint>
        <user-data-constraint>
            <transport-guarantee>NONE</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

    <security-role>
        <role-name>CUSTOMER</role-name>
    </security-role>
    <security-role>
        <role-name>COORDINATOR</role-name>
    </security-role>
    <security-role>
        <role-name>ADMIN</role-name>
    </security-role>

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
        </cookie-config>
    </session-config>

    <error-page>
        <exception-type>java.lang.Exception</exception-type>
        <location>/error.jsp</location>
    </error-page>

</web-app>
